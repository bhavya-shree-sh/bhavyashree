#include <stdio.h>

int main() {
int age;
    printf("enter a age: ");
    scanf("%d", &age);
    if(age<=17)
    printf("they are adults");
    else if(age<=50)
    printf("they are youngers ");
    else {
    printf("they are olders");
    }
    
     
    
}
